#!/usr/bin/env bash
set -euo pipefail

if [[ $# -eq 0 ]]; then
  exec cargo run -- start
fi

subcommand="$1"
shift || true

if [[ "${subcommand}" == "start" ]]; then
  exec cargo run -- start "$@"
fi

exec cargo "${subcommand}" "$@"
